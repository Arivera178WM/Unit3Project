CSS Grids ToDo:

Zac:
    PokeGrids_Info.css
        -Layouts for Info pages
        -Grid based
        -make sure all components go in grid

Jerry:
    PokeGrids_home.css
        -Layouts for index (home) page
        -Grid based
        -make sure all components go in grid

Aspen:
    PokeGrids_map.css
        -Layouts for site map page
        -Grid based
        -make sure all components go in grid
        -Design colors based on what you think looks best
Zac:
    PokeReset.css
        -Find a Reset file somewhere

Abe:
    PokeStyles.css

        -Ensure all styles apply to the correct tags for what the HTML file will be
        -This one is only for all styles unless it's not possible, then do one for each layout type

HTML ToDo:

Zac:
    All Files.html
        -Remove all CSS from HTML pages - Done
        -Redesign pages to match a grid style - Info only left
        -Link the appropiate CSS pages to each HTML Page - Done